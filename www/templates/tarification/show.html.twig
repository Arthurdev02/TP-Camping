{% extends 'base.html.twig' %}

{% block title %}Tarification{% endblock %}

{% block body %}
    <h1>Tarification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarification.id }}</td>
            </tr>
            <tr>
                <th>Tarif</th>
                <td>{{ tarification.Tarif }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ tarification.dateDebut ? tarification.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ tarification.dateFin ? tarification.dateFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tarification_index') }}">back to list</a>

    <a href="{{ path('app_tarification_edit', {'id': tarification.id}) }}">edit</a>

    {{ include('tarification/_delete_form.html.twig') }}
{% endblock %}
